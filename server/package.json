{
  "name": "kol-emet-server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "seed": "node scripts/seed.js",
    "migrate:blocks": "node scripts/migrate-blocks.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@simplewebauthn/server": "^13.3.0",
    "bcrypt": "^6.0.0",
    "connect-mongo": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.22.2",
    "express-session": "^1.19.0",
    "mongoose": "^8.24.1",
    "openai": "^6.34.0",
    "zod": "^3.23.8"
  },
  "resolutions": {
    "_comment_qs": "CVE-2026-82562 (array-limit bypass) is fixed in qs 6.16.0, but express 4 pins ~6.15.1. Forced here rather than moving to express 5, which is a breaking major. Revisit when express 4 widens the range; verify query-string parsing if this is changed.",
    "qs": "6.16.0"
  }
}
