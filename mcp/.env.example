# Local dev — matches server/src/index.js default port
# API_BASE=http://localhost:3004

# Production (Railway)
API_BASE=https://api.kol-emet.danielecker.dev

# Must match the BEARER_TOKEN environment variable set in Railway.
# For local dev, must match BEARER_TOKEN in server/.env.
BEARER_TOKEN=change-me
