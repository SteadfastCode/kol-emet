# Kol Emet — Wishlist

Unordered backlog of ideas and nice-to-haves. Not committed to, just captured.

## Frontend
- Rich text / markdown rendering in entry body (currently plain text)
- Entry linking — reference another entry by title, rendered as a clickable link
- Keyboard shortcuts (e.g. `/` to focus search, `Esc` to close expanded card)
- Mobile-optimized layout
- Animated expand/collapse on entry cards

## API & Data
- Soft delete (archive instead of hard delete)
- Entry revision history
- Bulk import endpoint (for future seed/migration needs)

## MCP / AI
- Claude-assisted entry drafting — suggest body content based on title + category
- Cross-entry relationship suggestions ("this entry mentions Zhalek — link to Zhalek Xitren?")
- Open question resolution flow — Claude flags when an open question may have been answered

## Productization
- User accounts with per-tenant wikis
- Invite-only access for collaborators (e.g. co-authors)
- Separate product name and brand under Steadfast Code
- Public template gallery (pre-seeded wiki structures for common use cases)
- Local AI support via Ollama (MCP layer already designed to be model-agnostic)
